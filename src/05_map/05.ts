// import {ManType} from './05.test';
// import {GovernmentBuildingsType, HousesType} from '../02_objects/02.test';
//
// export const transformator = (value: ManType[]) => {
//     return value.map(m => ({stack: [], firstName: m.name.split(' ')[0], lastName: m.name.split(' ')[1]}))
// }
//
// export const getGreeting = (value: ManType[]) => {
//     return value.map(m => `Hello ${m.name.split(' ')[0]}. Welcome to IT-Incubator`)
// }
//
// export const getStreets = (value: GovernmentBuildingsType[]) => {
//     return value.map(m => m.address.street.title)
// }
//
// export const getTitleStreets = (value: HousesType[]) => {
//     return value.map(m => m.address.street.title)
// }